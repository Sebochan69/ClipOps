# Moment detection v1

Given validated transcript segments, return exactly six or more candidate moments. Each candidate must provide `start_seconds`, `end_seconds`, `transcript_excerpt`, and a concise user-facing `reason_selected`. Do not reveal chain-of-thought. Respect the account profile's safety rules.
