[project]
name = "clipops-backend"
version = "0.1.0"
description = "ClipOps API"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
]

[dependency-groups]
dev = ["httpx", "pytest", "ruff"]

[tool.ruff]
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["src"]
