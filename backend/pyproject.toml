[project]
name = "clipops-backend"
version = "0.1.0"
description = "ClipOps API"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
]

[dependency-groups]
dev = ["httpx", "pytest"]

[tool.pytest.ini_options]
pythonpath = ["src"]
