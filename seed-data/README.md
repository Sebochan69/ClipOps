# Seed data

`accounts.json`, `briefs.json`, and `demo-transcript.md` are fictional demo inputs. Load them with `cd backend && uv run python -m clipops.seed`.

Only fictional data belongs here. Simulated performance data must be labeled.
