# Workflow observability

`GET /workflow-runs` exposes each run's status, concise failure message, and model provider/prompt/validation/repair metadata. The frontend shows this local run history; it is not distributed tracing.
